// const fn = (obj)=>{
//     console.log(`距离最近的地铁是${obj.name},打的费是${obj.price}`)
// }
// fn({name:'刘村',price:11})

const fn = ({name,price})=>{
    console.log(`距离最近的地铁是${name},打的费是${price}`)
}
fn({name:'刘村',price:11})
